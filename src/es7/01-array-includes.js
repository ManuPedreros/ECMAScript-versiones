// Includes nos permite trabajar sobre un array y saber si contiene un elemento

let numero = [1, 2, 3, 4, 5, 6, 7];
console.log(numero.includes(4));

const list = ['Oscar', 'David', 'Ana'];
console.log(list.includes('David'));
console.log(list.includes('david'));